{
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/js-yaml": "^4.0.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.3"
  },
  "name": "probi-oh",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
      "build": "tsc -p tsconfig.json",
      "build:test": "tsc -p tsconfig.test.json",
      "test": "npm run build:test && jest",
      "test:single": "npm run build:test && jest -t",
      "test:coverage": "npm run build:test && jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "idb": "^8.0.0",
    "js-yaml": "^4.1.0"
  }
}
